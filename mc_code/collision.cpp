
arma::cx_mat<arma::cx_mat> Collision::cmatrixCalc(){
  //the C matrix is made up of a different matrix for each pair of channels
  arma::cx_mat<arma::cx_mat> cmatrix(num_chanels,num_channels);
  
  
}
